spring:
  application:
    name: simulator
  artemis:
    host: localhost
    port: 61616
    user: admin
    password: admin
    mode: native
jms:
  queue:
    destination: testQueue
simulator:
  uriBase: http://localhost:5000

